#------------------------------------------------------------------------------
# Windows 64-bit AdaptiveCpp C++ compiler settings
#------------------------------------------------------------------------------

include $(GENERAL_RULES)/Sycl/c++

c++ARCH    := -m64

ifneq (,$(strip $(WM_COMPILE_OPTION)))
    sinclude $(DEFAULT_RULES)/c++$(WM_COMPILE_OPTION)
endif

# No -fPIC on Windows (position independent by default)
c++FLAGS    = \
    $(c++ARCH) $(GFLAGS) $(c++WARN) $(c++OPT) $(c++DBUG) $(ptFLAGS) \
    $(ACPP_FLAGS) $(FOAM_EXTRA_CXXFLAGS) $(LIB_HEADER_DIRS)

Ctoo        = $(WM_SCHEDULER) $(CC) $(c++FLAGS) -c $< -o $@
cctoo       = $(Ctoo)
cpptoo      = $(Ctoo)
cxxtoo      = $(Ctoo)

include $(GENERAL_RULES)/Sycl/link-c++

#------------------------------------------------------------------------------
