if [ -z "$have_cuda" ]
then

    export WM_EXCLUDE_HEADERS="$WM_EXCLUDE_HEADERS -icudaError.cuh \
    -icudaExecutor.cuh \
    -icudaAtomic.cuh \
    -icudaDeviceInit.cuh \
    -icudaMemoryExecutor.cuh \
    -icudaDeviceUtils.cuh"

fi

if [ -z "$have_hip" ]
then

    export WM_EXCLUDE_HEADERS="$WM_EXCLUDE_HEADERS -ihipError.hpp \
    -ihipExecutor.hpp \
    -ihipAtomic.hpp \
    -ihipDeviceInit.hpp \
    -ihipMemoryExecutor.hpp \
    -ihipDeviceUtils.hpp"

fi
