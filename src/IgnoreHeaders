if [ -z "$have_cuda" ]
then

    export WM_EXCLUDE_HEADERS="$WM_EXCLUDE_HEADERS -icudaError.cuh \
    -icudaExecutor.cuh \
    -icudaAtomic.cuh \
    -icudaDeviceInit.cuh \
    -icudaMemoryExecutor.cuh"

fi

if [ -z "$have_hip" ]
then

    export WM_EXCLUDE_HEADERS="$WM_EXCLUDE_HEADERS -ihipError.hpp \
    -ihipExecutor.hpp \
    -ihipAtomic.hpp \
    -ihipDeviceInit.hpp \
    -ihipMemoryExecutor.hpp"

fi

if [ -z "$have_sycl" ]
then

    export WM_EXCLUDE_HEADERS="$WM_EXCLUDE_HEADERS -isyclExecutor.H \
    -isyclAtomic.H \
    -isyclDeviceInit.H \
    -isyclMemoryExecutor.H"

fi
